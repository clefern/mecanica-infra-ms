# helm — Charts dos Microsserviços

> Frente B (Gabriel + Cristian) — a preencher.

Charts esperados:
- **RabbitMQ** (`bitnami/rabbitmq`) — Helm release no EKS
- **MongoDB** (Atlas ou `bitnami/mongodb`) — para workshop-service
- Configurações de persistência, credenciais via K8s Secrets

Referência de como fazer release Helm: ver `infra-k8s/` da Fase 3.
